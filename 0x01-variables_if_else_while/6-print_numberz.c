#include <stdio.h>
/**
 * main - Entry Point
 *
 * Description: Gets the last number of a random digit and Checks it
 *
 * Return: Returns Zero
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; ++c)
		putchar(c);
	putchar('\n');

	return (0);
}
