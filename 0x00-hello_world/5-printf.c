#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description: Prints out a message using the printf function
 *
 * Return: Returns 0 always
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
